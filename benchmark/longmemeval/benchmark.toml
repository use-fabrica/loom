name = "longmemeval"
kind = "e2e"

[dataset]
source = "xiaowu0162/longmemeval-cleaned"
revision = "98d7416c24c778c2fee6e6f3006e7a073259d48f"

[judge]
# Exact pinned model string used by the official evaluate_qa.py, see
# model_zoo['gpt-4o'] at evaluate_qa.py:14 (pinned repo SHA, see README).
model = "gpt-4o-2024-08-06"
prompt_version = "v1"

[answerer]
# Not yet pinned: the harness refuses real e2e runs until this is set.
# See README "Paper answerer setup" for what the original paper used.
model = ""

[entrypoints]
fetch = "fetch.py"
score = "score.py"
