[project]
name = "longmemeval"
version = "0.1.0"
description = "LongMemEval end-to-end benchmark adapter for the Loom harness"
requires-python = ">=3.12"
dependencies = [
    "huggingface_hub==1.29.0",
    "openai==3.6.0",
    "backoff==2.2.1",
    "tqdm==4.70.0",
    "numpy==2.5.2",
]

[tool.uv]
package = false
